<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:actipro="http://schemas.actiprosoftware.com/avaloniaui"
        x:Class="Pikura.Avalonia.Views.Dialogs.RangePickerDialog"
        Title="Pick Range"
        Width="440" Height="230"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Container2BackgroundBrush}">

    <StackPanel Margin="24,20" Spacing="14">
        <TextBlock x:Name="TitleText" FontWeight="SemiBold" FontSize="14"/>
        <TextBlock x:Name="DescText" FontSize="12" Foreground="{DynamicResource DefaultForegroundBrushTertiary}"
                   TextWrapping="Wrap"/>
        <TextBox x:Name="RangeInput"
                 PlaceholderText="e.g. 1-20 or 1,5-10,50"
                 CornerRadius="6" FontSize="13"/>
        <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="OnCancel"/>
            <Button Content="OK" Classes="accent" Click="OnOk"/>
        </StackPanel>
    </StackPanel>
</Window>
